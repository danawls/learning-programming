#include <iostream>
#include <deque>
#include <vector>

using namespace std;
typedef deque<int> dint;
typedef vector<int> vint;

int main()
{
    // dint di1;
    // for (long long i = 1; i <= 1000000000; ++i)
    // {
    //     di1.push_back(i);
    // }

    vint vi1;
    for (long long i = 1; i <= 1000000000; ++i)
    {
        vi1.push_back(i);
    }
    return 0;
}